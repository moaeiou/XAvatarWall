name: Don't do sexy

on:
  push:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    name: Build for ${{ matrix.dickname }}
    runs-on: ubuntu-latest
    env:
      pn: xavatarwall
    strategy:
      fail-fast: false
      matrix:
        include:
          - tag: "GOAMD64=v4"
            dickname: "linux-amd64v4-glibc"
          - tag: "GOAMD64=v3"
            dickname: "linux-amd64v3-glibc"
          - tag: "GOAMD64=v2"
            dickname: "linux-amd64v2-glibc"
          - tag: "GOAMD64=v1"
            dickname: "linux-amd64v1-glibc"
          - tag: ""
            dickname: "linux-amd64-glibc"
          - tag: "CGO_ENABLED=0"
            dickname: "linux-amd64-musl"
          - tag: "GOARCH=arm64"
            dickname: "linux-arm64-musl"
          - tag: "GOOS=darwin GOARCH=arm64"
            dickname: "darwin-arm64"
          - tag: "GOOS=windows GOARCH=amd64"
            dickname: "windows-amd64.exe"

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Fuck cache
        shell: bash
        run: |
          mv go.mod go.mod.backup

      - name: Set up Go
        uses: actions/setup-go@main
        with:
          go-version: "stable"

      - name: Build!
        shell: bash
        run: |
          mv go.mod.backup go.mod
          ${{ matrix.tag }} go build -o ${{ env.pn }}-${{ matrix.dickname }} .

      - name: Release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@master
        with:
          tag_name: V0.0.7
          files: ${{ env.pn }}-${{ matrix.dickname }}
          body: |
            # 📝 Release Note
            Fixes and improvements
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}